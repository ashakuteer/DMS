You are working on my NGO Donation Tracker (Next.js frontend + NestJS backend + Prisma). Everything is running, but donor “Assign To staff” is not correct and I’m getting “Failed to fetch” from src/app/(dashboard)/dashboard/donors/[id]/components/AssignDonorOwner.tsx when calling:

${process.env.NEXT_PUBLIC_API_URL}/users/staff (with credentials include)

Goal (do ONLY these)

Fix /users/staff fetch issue

Ensure the frontend fetch URL is correct considering backend has a global /api prefix.

Make sure NEXT_PUBLIC_API_URL usage is correct (no double /api, no missing /api).

Ensure cookies/credentials work (credentials: "include") and no CORS/auth issue.

The endpoint should return JSON list of staff users reliably.

Assign donor at CREATE time (donor form page)

On /dashboard/donors/new (Add New Donor form), the Assign To dropdown must show real assignable staff.

When saving donor, it must send assignedToUserId to backend so donor is assigned immediately at creation time.

Keep “Unassigned” option.

Show staff NAMES (not roles like “Staff Member”, “Telecaller User”)

The dropdown must show staff as actual names:

Rajini, Ashwini, Nandini, Sarala, Padma, Revathi, Swapna, Admin, Other

If these users do not exist in DB, create a safe approach:

either seed/create them automatically (only if missing), OR

build an “Admin Staff Directory” table in DB and use that list.

Do NOT show generic role labels; show names.

Admin must manage staff & reassignment

Add an Admin-only screen (simple) to:

Add/disable staff contacts (name + phone + email optional)

When someone stops working, Admin can reassign donors from old staff to another staff in bulk.

This should integrate with donor assignment (assignedToUserId) so reassignment updates donors properly.

Donor picture feature

Add donor photo upload while creating donor:

store image (DB field with URL/path) and show it on donor detail page.

validate file type and size.

keep it optional.

Important constraints

Do not change unrelated code or refactor large files.

Keep existing conventions and structure.

Fix permission issue cleanly: the permission string used is DONOR_ASSIGN_OWNER (same in frontend + backend). Make sure admins can always access.

Deliverables

Working dropdown shows correct staff names.

No “Failed to fetch” error.

Donor assigned during donor creation.

Admin screen for staff manage + bulk donor reassignment.

Donor photo upload works end-to-end.