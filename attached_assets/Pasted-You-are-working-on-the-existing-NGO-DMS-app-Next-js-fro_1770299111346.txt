You are working on the existing “NGO DMS” app (Next.js frontend + NestJS backend + Prisma/Postgres + Supabase storage). Implement a complete Beneficiaries Module with profiles for:

Orphan Girls Home

Visually Challenged Boys Home

Old Age Home

A) Data Model (Prisma)

Add these models (or extend if already exists). Use UUIDs for IDs.

Beneficiary

id (uuid, pk)

code (string, unique) like AKF-BEN-000001

fullName (string, required)

gender (enum: MALE/FEMALE/OTHER, optional)

category (enum: ORPHAN_GIRL / BLIND_BOY / SENIOR_CITIZEN, required)

home (enum: ORPHAN_GIRLS_HOME / BLIND_BOYS_HOME / OLD_AGE_HOME, required)

dobMonth (int 1-12, optional)

dobDay (int 1-31, optional)

joinDate (date, optional)

ageApprox (int, optional) // used if year not known

educationClassOrStatus (string, optional) // class / college / NA for seniors

schoolOrCollege (string, optional)

healthNotes (text, optional)

background (text, optional)

hobbies (string, optional)

dreamCareer (string, optional)

favouriteSubject (string, optional)

favouriteActivity (string, optional)

funFact (string, optional)

notes (text, optional)

protectPrivacy (boolean, default false) // hide profile details in public exports

photoUrl (string, optional)

createdAt, updatedAt

BeneficiaryUpdate

id (uuid)

beneficiaryId (fk)

title (string)

description (text)

updateDate (date default now)

photoUrl (string, optional)

createdByUserId (fk)

createdAt

Sponsorship
Links donors to a beneficiary (one beneficiary can have multiple sponsors; one donor can sponsor multiple beneficiaries).

id (uuid)

donorId (fk)

beneficiaryId (fk)

type (enum: EDUCATION / FOOD / MEDICAL / GROCERIES / MONTHLY_SUPPORT / FESTIVAL / OTHER)

amount (decimal, optional)

inKindItem (string, optional) // eg: “Rice 25kg”

frequency (enum: ONE_TIME / MONTHLY / QUARTERLY / YEARLY / ADHOC)

startDate (date, optional)

endDate (date, optional)

active (boolean default true)

notes (text, optional)

createdAt

BeneficiaryTimelineEvent
Unified timeline items for beneficiary:

id (uuid)

beneficiaryId (fk)

eventType (enum: PROFILE_CREATED / UPDATE_ADDED / SPONSORSHIP_ADDED / DONATION_LINKED / NOTE / PHOTO_UPDATED)

title (string)

description (text, optional)

eventDate (date default now)

createdByUserId (fk)

createdAt

Also ensure existing Donor model can relate to Sponsorship.
Run Prisma migration.

B) Supabase File Upload

Use existing Supabase integration.
Create storage buckets (if not existing):

beneficiary-photos

beneficiary-updates

Implement upload helpers for:

beneficiary profile photo

update photo attachment

C) Backend APIs (NestJS)

Create Beneficiaries module with controllers/services:

Beneficiaries CRUD

GET /api/beneficiaries with filters: home, category, search (name/code), privacy toggle (admins only can see protected fields)

POST /api/beneficiaries

GET /api/beneficiaries/:id

PATCH /api/beneficiaries/:id

DELETE /api/beneficiaries/:id (soft delete preferred if app already uses soft deletes)

Upload endpoints (or reuse existing signed URL approach)

POST /api/beneficiaries/:id/photo → upload + store photoUrl

POST /api/beneficiaries/:id/updates/:updateId/photo

Updates

GET /api/beneficiaries/:id/updates

POST /api/beneficiaries/:id/updates

DELETE /api/beneficiaries/:id/updates/:updateId

Sponsorships

GET /api/beneficiaries/:id/sponsors

POST /api/beneficiaries/:id/sponsors (select donor + sponsorship fields)

PATCH /api/sponsorships/:id (edit, deactivate)

DELETE /api/sponsorships/:id

Timeline

GET /api/beneficiaries/:id/timeline
Auto-create timeline events whenever profile/update/sponsorship/photo changes.

D) Frontend (Next.js Dashboard UI)

Add a full Beneficiaries section in sidebar:

Beneficiaries List Page /dashboard/beneficiaries

Tabs or filters for 3 Homes

Search box by name/code

Table columns: Code, Name, Home, Category, DOB (MM/DD if available), Active Sponsors count, Last Update date, Actions (View)

“Add Beneficiary” button

Add/Edit Beneficiary Page /dashboard/beneficiaries/new and edit via modal or page

Form similar to provided sample UI:

Photo upload

Basic info + aspirations/social fields

DOB should be Month & Day only (no year). Use 2 dropdowns (Month, Day)

If DOB unknown, allow blank and use AgeApprox

“Protect Privacy” checkbox

Beneficiary Profile Page /dashboard/beneficiaries/[id]
Top summary cards:

Sponsors count

Linked donors count

Updates count

Next birthday (if MM/DD present)
Tabs:

Overview

Timeline

Updates (add/update/delete, attach photo)

Sponsors (link donor, list sponsors, deactivate sponsorship)

Communication (manual templates preview)

Log (if app already has log pattern)

E) Link Donor ↔ Beneficiary (Sponsor workflow)

From Beneficiary profile → Sponsors tab:

Button: “Add Sponsor”

Search donor by name/phone/code

Save Sponsorship + create timeline events on both donor and beneficiary sides if donor timeline exists.

From Donor profile add a section: “Sponsored Beneficiaries”

list beneficiaries linked via Sponsorship

clicking opens beneficiary profile

F) Birthday & Auto-email logic (no WhatsApp automation)

We keep WhatsApp manual only.

Donor birthday email (already exists). Enhance:

If donor sponsors any beneficiaries: include 1 featured beneficiary photo + short message line (“On your special day, you’re making a difference for <beneficiary name>…”)

If multiple beneficiaries, pick the one with most recent update, else first.

Beneficiary birthday

Create new reminder type: BENEFICIARY_BIRTHDAY

On beneficiary profile, if dobMonth & dobDay are present:

create reminders for sponsors: 30d, 15d, 7d, 2d, same day (same pattern as donor special days)

Auto-email goes to sponsor donors (only donors with email)

WhatsApp remains manual “Open WhatsApp” only.

Email template: thank the donor and share a short update about beneficiary (use last update title/desc if exists).

G) Templates (Admin settings)

Add templates for:

Beneficiary Birthday Thank-You Email (to Donor)

Donor Birthday Wish (with Beneficiary mention)
Allow variables:

{{donorName}}

{{beneficiaryName}}

{{homeName}}

{{beneficiaryUpdateSnippet}}

{{sponsorshipType}}

{{foundationName}}

If Templates module already exists, integrate there; otherwise create a simple DB table EmailTemplate with key + subject + body.

H) Export Excel (Future-ready)

Add endpoint + UI button:

GET /api/beneficiaries/export.xlsx

Download an Excel with columns:
Code, FullName, Home, Category, DOB(MM/DD), AgeApprox, JoinDate, SchoolOrCollege, ClassOrStatus, HealthNotes, Hobbies, DreamCareer, Notes, SponsorsCount, SponsorDonorCodes (comma separated)
Respect ProtectPrivacy: if protectPrivacy=true, exclude sensitive fields (healthNotes/background) and mask fullName to initials.

I) Permissions

Only ADMIN can:

create/edit/delete beneficiary

upload photos

export excel
Staff can view list/profile.

J) Fix any missing shadcn components automatically

If build errors occur for missing UI components (like switch/radio-group), add the missing shadcn UI component files into src/components/ui/ and ensure imports resolve.

K) Acceptance Tests (manual checklist)

Can create beneficiary with photo, month/day DOB, privacy toggle.

Can add updates with photo; timeline shows them.

Can link donor as sponsor; sponsor appears in donor profile too.

Beneficiary birthday reminders created for sponsor donors (30/15/7/2/0).

Auto-email respects global Email Settings toggle + SMTP settings.

Export beneficiaries Excel downloads properly.

No route shows blank page; all API calls handled with toasts.

Implement everything end-to-end, including migrations, backend routes, frontend pages, and integration with existing reminders/email scheduler.