YOU ARE WORKING INSIDE MY NGO DMS REPLIT PROJECT (Next.js app + Nest API + Prisma DB). Implement ALL items below in one go. Keep changes minimal, stable, and production-safe. After finishing, run the app and ensure no build/runtime errors.

========================
TOP PRIORITY GOALS (1,2,3)
========================

(1) DAILY ACTIONS BUTTONS (Special Days + Pledges) must work correctly
(2) SPECIAL DAYS + AUTO REMINDERS (yearless dates; reminder schedule 30/15/7/2/0)
(3) PLEDGES: add pledge + fulfill + postpone must work reliably

Also: Fix any missing UI components (example: "@/components/ui/radio-group") and any click-behavior bugs where button clicks navigate to donor profile.

------------------------------------------------------------
A) DAILY ACTIONS: FIX ACTION BUTTONS + ADD EMAIL ICON
------------------------------------------------------------
A1) In /dashboard/daily-actions:
- For Special Days tables (Today’s Special Days + Upcoming Special Days):
  - Show 3 action icons per row:
    1) WhatsApp (opens WhatsApp message)
    2) Email (opens email compose)
    3) Open Profile (navigates to donor profile)
- IMPORTANT: Clicking WhatsApp or Email MUST NOT navigate to profile. Only “Open Profile” navigates.
  - If the row currently has an onClick handler, ensure action buttons call e.stopPropagation() and do their own action.
- WhatsApp must open a wa.me link with a COMPLETE template message (not just “Dear Name,”).
- Email: For now, do NOT try to send server-side email. Instead open the user’s mail client:
  - Use mailto: donorEmail?subject=...&body=...
  - If donor has no email, show toast: “No email address found for this donor.”
- After opening WhatsApp or Email, show a toast like:
  - “WhatsApp message opened”
  - “Email draft opened”
- OPTIONAL (nice): Add a “Mark Done ✓” small action or auto-log the action in Communication Log (see section E).

A2) In the Pledges Due table (Daily Actions):
- Keep these actions:
  - Fulfill (✓) -> marks pledge fulfilled
  - Postpone (calendar icon) -> opens a date picker modal, then updates expected date
  - WhatsApp -> opens WhatsApp follow-up template
  - Open Profile -> navigates to donor profile
- Again: button clicks must not trigger profile navigation unless “Open Profile” icon is clicked.

------------------------------------------------------------
B) SPECIAL DAYS MUST BE YEARLESS (Month + Day only)
------------------------------------------------------------
B1) Update data model for special days:
- We should NOT store year for donor special days (birthdays/anniversaries/memorial days).
- Store:
  - month (1-12)
  - day (1-31)
  - type (BIRTHDAY | ANNIVERSARY | MEMORIAL | OTHER)
  - label/title (optional: “Wedding Anniversary”, “Late Father Memorial”)
  - notes (optional)
- Migration:
  - If existing special days currently store full date, keep them but also backfill month/day from it.
  - Going forward, UI should only ask for month and day (no year).
- Update all UI forms for donor special days (Donor profile > Special Days tab) to capture ONLY month/day + type + label.
  - Use dropdown Month + dropdown Day, OR a single MM-DD input.
- Update display:
  - Show as “3 Feb” (no year)
  - For “days left” logic, compute the NEXT OCCURRENCE date (this year or next year) from month/day.

------------------------------------------------------------
C) AUTO REMINDERS: 30 / 15 / 7 / 2 / SAME DAY
------------------------------------------------------------
C1) Reminder rules for special days:
For each special day occurrence, reminders must be created/shown at:
- 30 days before
- 15 days before
- 7 days before
- 2 days before
- 0 days (same day)
These should appear in Reminders page and Daily Actions.

C2) Implement generation safely:
- If there is a “Generate Reminders” button, it must never crash.
- It should:
  - Compute next occurrence for every special day
  - For each offset (30,15,7,2,0), compute dueDate = occurrenceDate - offset
  - Only create reminders where dueDate is within a reasonable window (example: within next 60 days OR allow viewing by tabs 7/30/Overdue).
- Avoid duplicates:
  - Use unique constraint or logic so the same donor+specialDay+offset+occurrenceYear doesn’t create duplicates.
- Tabs should show counts correctly (Today / Next 7 / Next 30 / Overdue).

C3) Daily Actions should show:
- Today’s special days (occurring today) and upcoming (next 7 days)
- Use yearless special days logic to calculate.

------------------------------------------------------------
D) PLEDGES: ADD + FULFILL + POSTPONE MUST WORK
------------------------------------------------------------
D1) Donor profile > Pledges tab:
- Ensure “Add Pledge” button exists and works.
- Fields:
  - item/category (ex: Rice/Groceries/School Fees/Monthly Support)
  - quantity (optional) + unit (optional) OR amount (optional)
  - expectedDate (required)
  - notes (optional)
  - status default = PENDING
- After saving, pledge appears in list.

D2) Fulfill pledge:
- Fulfill button sets status to FULFILLED, sets fulfilledAt timestamp.
- UI updates instantly with toast “Pledge marked as fulfilled”.

D3) Postpone pledge (FIX CURRENT FAILURE):
- When Postpone icon clicked:
  - open a modal with date picker + optional note (“Reason”)
  - on confirm, call backend endpoint correctly (match the existing Nest route):
    - Use the route you already have mapped in logs: /api/pledges/:id/postpone
  - Send JSON body: { expectedDate: "YYYY-MM-DD", note?: "..." }
  - Show toast on success: “Pledge postponed”
  - If failure, show toast with safe error: “Failed to postpone pledge”
- Make sure frontend and backend agree on HTTP method (POST/PATCH) and body.
- Ensure CORS/dev origins are not breaking fetch.

D4) “Pledges Due” logic:
- A pledge is “due” if expectedDate <= today AND status is PENDING
- Or show “due in X days” for expectedDate in near future.

------------------------------------------------------------
E) TEMPLATES (WhatsApp + Email) — REQUIRED
------------------------------------------------------------
E1) Add Templates storage (DB or config) for:
- Special Day WhatsApp template
- Special Day Email subject + body
- Pledge Follow-up WhatsApp template
- Pledge Follow-up Email subject + body (optional)
If you already have a Templates page, add these fields there.

E2) Use tokens/variables:
- {{donorName}}
- {{occasion}} (Birthday/Anniversary/Memorial)
- {{date}} (e.g., 3 Feb)
- {{orgName}} (Asha Kuteer Foundation)
- {{pledgeItem}}, {{pledgeQty}}, {{pledgeUnit}}, {{expectedDate}}
- {{contactPhone}} (optional)

E3) Provide DEFAULT templates if none saved:
Special Day WhatsApp default:
"Dear {{donorName}},\nWarm wishes from {{orgName}} on your {{occasion}} ({{date}}). 🎉\nThank you for your continued support.\n– Team {{orgName}}"

Pledge WhatsApp default:
"Dear {{donorName}},\nA gentle reminder from {{orgName}} about your pledge: {{pledgeItem}} {{pledgeQty}} {{pledgeUnit}} expected on {{expectedDate}}.\nWhenever convenient, please let us know. 🙏\n– Team {{orgName}}"

Special Day Email default:
Subject: "Warm wishes on your {{occasion}}"
Body:
"Dear {{donorName}},\n\nWarm wishes from {{orgName}} on your {{occasion}} ({{date}}).\nThank you for being a valued supporter.\n\nRegards,\n{{orgName}}"

E4) WhatsApp link:
- Use https://wa.me/<countrycode+number>?text=<encoded message>
- Normalize phone numbers safely; if missing, toast “No phone number found”.

------------------------------------------------------------
F) FIX MISSING UI COMPONENTS / BUILD ERRORS
------------------------------------------------------------
If you see build error like:
"Module not found: Can't resolve '@/components/ui/radio-group'"
then:
- Either add the missing file at the expected path (components/ui/radio-group.tsx) using shadcn pattern,
OR update imports to an existing component path.
Do not leave the app in broken build state.

Also fix any runtime errors like "userRole is not defined" by ensuring variables are defined or safely optional.

------------------------------------------------------------
ACCEPTANCE TESTS (MUST PASS)
------------------------------------------------------------
1) Open /dashboard/daily-actions:
   - Special Days rows show WhatsApp + Email + Open Profile icons.
   - Clicking WhatsApp opens WhatsApp with a full message (not just “Dear Name,”) and DOES NOT navigate.
   - Clicking Email opens mail draft and DOES NOT navigate.
   - Clicking Open Profile navigates to donor profile.

2) In Pledges Due:
   - Postpone opens modal, selecting new date updates pledge and shows success toast.
   - Fulfill works and updates status.

3) Donor profile > Special Days:
   - Add special day using only month/day (no year).
   - Reminders generated at 30/15/7/2/0 and show correctly in Reminders page.

4) No Next.js build errors. No missing module errors.

========================
NEXT FEATURE (AFTER ABOVE IS STABLE): BENEFICIARIES + SPONSOR LINKING
========================
Create Beneficiaries module similar to my sample screenshots.

G1) Add Beneficiary entity:
- Fields similar to sample form:
  - firstName (required)
  - gender (optional)
  - classInSchool (optional)
  - schoolCollege (optional)
  - approxYearsWithNgo (optional)
  - childOriginPlace (optional)
  - dateOfBirth (optional; for beneficiaries full date is OK)
  - background/notes (optional)
  - dreamCareer, hobby, favouriteSubject, bestFriend, favouriteCelebration, favouriteGame, favouriteActivityAtNgo, sourceOfPrideOrHappiness, funFact, additionalNotes (all optional)
  - photo (optional)
  - protectPrivacy boolean
  - homeType (GIRLS_HOME | BLIND_BOYS_HOME | OLD_AGE_HOME) and homeLocation (optional)
- Pages:
  - Beneficiaries list page with search + count
  - Add Beneficiary page (form like sample)
  - Beneficiary profile page (like donor profile style tabs)

G2) Link Donor ↔ Beneficiary (Sponsorship):
- Create Sponsorship model:
  - donorId
  - beneficiaryId
  - sponsorshipType (Education/Monthly/Medical/Ration/etc)
  - amount or item details
  - startDate, endDate (optional)
  - status (ACTIVE/PAUSED/ENDED)
  - notes
- In donor profile add a tab “Sponsored Beneficiaries”
- In beneficiary profile add a tab “Sponsors”
- Allow adding/removing sponsorship from either side.

Do G1/G2 only after A–F pass.

DELIVERABLE:
Make code changes and ensure everything compiles and runs. No broken pages.
