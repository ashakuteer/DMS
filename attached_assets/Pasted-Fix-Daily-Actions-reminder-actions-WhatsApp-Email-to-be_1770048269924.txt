Fix Daily Actions reminder actions (WhatsApp + Email) to be complete and allow marking reminders done.

Issues:
1) WhatsApp message is incomplete (only "Dear <name>," appears).
2) Clicking Email icon navigates to donor profile instead of sending.
3) After sending WhatsApp/Email for special days, we need a way to mark the reminder as done (or auto-mark on email send).

Fix requirements:

A) WhatsApp message completeness
- Ensure WhatsApp URL builder uses the correct template for reminder type:
  - Birthday: include donor name + wish + Asha Kuteer signature + contact
  - Anniversary: similar
  - Memorial: respectful template (no year needed)
  - Follow-up: follow-up template
  - Pledge due: pledge reminder template (include item + expected date)
- Make sure placeholders are resolved before encoding:
  {{donor_name}}, {{homes}}, {{phone1}}, {{phone2}}, {{website}}, {{email}}
- URL encode fully and verify text is not truncated.

B) Email button should send email, not navigate
- Remove any Link wrapper / row click navigation for action icons.
- Ensure Email icon is a <button type="button"> that calls sendEmail(reminderId).
- Add event.preventDefault() + event.stopPropagation() on all action buttons.
- Only the explicit "Open Profile" icon should navigate to donor profile.

C) Mark Done workflow
- Add/confirm buttons per reminder row:
  - ✅ Mark Done (calls POST /api/reminders/:id/complete)
  - ⏰ Snooze 7 days / 30 days (POST /api/reminders/:id/snooze)
- Auto-mark reminder DONE ONLY when Email send succeeds.
- For WhatsApp, do NOT auto-complete; user must click Mark Done manually after sending.

D) Logging
- WhatsApp click logs CommunicationLog: channel=WHATSAPP, status=TRIGGERED, reminderId
- Email send logs CommunicationLog: channel=EMAIL, status=SENT/FAILED, reminderId
- Mark done logs reminder completion.

E) Verification
1) Open Daily Actions → Upcoming Special Days → click WhatsApp:
   - Opens WhatsApp with full message (not only greeting)
   - Does not navigate away
2) Click Email:
   - Sends email successfully
   - Reminder auto-marks DONE
3) WhatsApp → click Mark Done manually:
   - Reminder disappears from list
