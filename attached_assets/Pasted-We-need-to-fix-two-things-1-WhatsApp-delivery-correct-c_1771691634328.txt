We need to fix two things:
1) WhatsApp delivery + correct configuration + clear status
•	We already send WhatsApp templates using Twilio Content API and show “Queued” in UI.
•	Sometimes backend logs show “accepted”, but Twilio Console shows “Undelivered”.
•	Improve our implementation so we can debug + show correct status.
Required changes:
1.	Validate env vars on startup and log clearly (without secrets):
o	TWILIO_ACCOUNT_SID must start with “AC”
o	TWILIO_AUTH_TOKEN must be present
o	TWILIO_MESSAGING_SERVICE_SID must start with “MG” (NOT “whatsapp:+…”)
o	TWILIO_WHATSAPP_NUMBER must start with “whatsapp:+”
o	TWILIO_DONATION_THANK_YOU_CONTENT_SID / REMINDER / OCCASION must start with “HX”
If any invalid, mark WhatsApp status as “Not Configured” and don’t attempt sending.
2.	When sending WhatsApp:
o	Ensure “to” is always E.164 with + (ex: +919700711700)
o	Log “to”, “from”, “contentSid”, “messagingServiceSid”, and Twilio response SID + status.
o	After creating the message, do NOT say “sent” immediately. Save CommunicationLog status = “accepted/queued” initially.
3.	Add a webhook endpoint for Twilio status callbacks:
o	POST /api/webhooks/twilio/status
o	Verify signature if possible (optional for now)
o	Update CommunicationLog status to delivered/undelivered/failed + errorCode/errorMessage.
o	Update donation API response to return last known WhatsApp status from CommunicationLog.
4.	Frontend post-donation dialog:
o	Keep it as “Donation Saved Successfully” with auto status lines only (Email + WhatsApp + receipt generated + Close).
o	Show WhatsApp as: Queued / Delivered / Undelivered / Failed / Not Configured (based on CommunicationLog).
o	Do NOT add manual send buttons here (manual resend stays in Donation Details page only).
2) Email template + PDF receipt design fixes
•	Email HTML:
1.	Make header logo larger and more visible.
2.	Under “Asha Kuteer Foundation” show:
“Healing Hearts – Shaping Lives”
“In the service of Mankind since 2013”
3.	Keep the green theme and background like current design.
•	PDF receipt:
1.	Use full green theme (not only green lines) — header/footer should have green background and consistent branding.
2.	Add Asha Kuteer logo in PDF header.
3.	Footer “Our Homes” must be 1 line (no wrap). If needed, reduce font size and use separators:
“Asha Kuteer Girls Home – Uppal, Hyderabad | Asha Kuteer Blind Boys Home – Begumpet, Hyderabad | Asha Kuteer Old Age Home – Boduppal, Hyderabad”
Important: Logo file exists at /public/asha-kuteer-logo.jpg. Use it in both email and PDF.
Deliverables:
•	Code changes for Twilio webhook + status tracking.
•	Updated email HTML template.
•	Updated PDF receipt generator.

