Implement next 3 features in one run:
1) Daily Action Inbox fully working
2) Pledge reminders + actions
3) Merge duplicate donors tool

Rules:
- Keep existing donors/donations/receipts/emails stable.
- Keep locked OLD receipt + OLD email formats unchanged.
- Use Prisma ORM only (no raw SQL).
- Maintain RBAC: Admin full, Staff limited, Telecaller read-only.

====================================================
1) DAILY ACTION INBOX (FIX "FAILED TO LOAD")
====================================================

A) Backend
- Create GET /api/daily-actions endpoint that returns:
  - todaySpecialDays: reminders due today (birthday/anniversary/memorial)
  - upcoming7: reminders due in next 7 days
  - upcoming15: reminders due in next 15 days
  - overdue: overdue reminders
  - pledgeDue: pledge reminders due/overdue
  - followUpDue: follow-up reminders due/overdue
  - atRiskDonors: donors with RED health status

- Use existing ReminderTask/Reminder tables and Pledge reminders.
- Response must be fast (single query per section or well-indexed).

B) Frontend
- Fix /dashboard/daily-actions to call /api/daily-actions and render:
  - Today
  - Next 7 days
  - Next 15 days
  - Overdue
  - Pledges Due
  - At-Risk Donors
- Each row should show: donor name, reason, due date, status
- Action buttons:
  - Open Donor
  - Send Email (manual)
  - Send WhatsApp (manual)
  - Mark Done (for reminders)
  - Snooze 7 days / 30 days

C) RBAC
- Admin + Staff: full view + actions
- Telecaller: view + WhatsApp only (no email)

====================================================
2) PLEDGE REMINDERS + ACTIONS
====================================================

A) Reminder generation
For each Pledge with status=PENDING:
- Create ReminderTasks at:
  - 7 days before expectedFulfillmentDate
  - 2 days before
  - On expectedFulfillmentDate
- If overdue, it must show in Overdue.
- Avoid duplicates (unique key: donorId + pledgeId + dueDate + offsetType)

B) UI actions inside Daily Actions + Reminders + Donor Pledges tab
- Mark Fulfilled (changes pledge status + closes reminders)
- Postpone (updates expected date + regenerates reminders cleanly)
- Cancel (status CANCELLED + closes reminders)

C) Logging
- Log pledge reminder generation (system)
- Log pledge status changes (admin/staff)

====================================================
3) MERGE DUPLICATE DONORS TOOL (ADMIN ONLY)
====================================================

A) Duplicate detection
- Create Admin page: /dashboard/donors/duplicates
- Detect duplicates by:
  - same phone
  - same email
  - similar name (optional simple fuzzy match)
- Show duplicate groups with donors listed.

B) Merge flow (Admin only)
For each duplicate group:
- Choose a "Primary Donor" (keep)
- Select other donors to merge into primary
- On merge:
  - Move ALL related data to primary:
    - donations
    - pledges
    - reminders
    - communication logs
    - family members
    - special days
  - Delete merged donor records OR mark them as merged/archived (preferred: soft delete)
  - Ensure donor code of primary remains
- Show merge summary and log the merge action.

C) Safety
- Require confirmation checkbox: "I understand this cannot be undone"
- Create an audit log entry with admin user + time + donors merged.

====================================================
Verification checklist (must pass):
1) Daily Actions page loads (no failed to load)
2) Pledge due reminders appear automatically
3) Mark Fulfilled/Postpone closes/updates reminders correctly
4) Duplicate donors page shows groups
5) Merge works and all data transfers correctly
