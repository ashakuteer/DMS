We have a monorepo with Next.js frontend (port 5000) and NestJS backend (port 3001). Frontend loads, login page loads, but all API calls fail with ECONNREFUSED because the NestJS API crashes on startup due to Prisma.

Current error (crash):
PrismaClientInitializationError: Can't reach database server at db.ovxfbfcrtwrhfugvwjym.supabase.co:5432 (P1001)
This happens during Nest bootstrap: apps/api/src/prisma/prisma.service.ts:7 (onModuleInit / prisma connect).

Request:

Please fix this without rewriting the whole app. We just need the backend to start reliably and connect to Supabase DB from Replit.

Tasks to perform:

Diagnose network/DB connectivity from Replit

From shell run:

nslookup db.ovxfbfcrtwrhfugvwjym.supabase.co

nc -vz db.ovxfbfcrtwrhfugvwjym.supabase.co 5432 (or equivalent connectivity check)

Confirm if Replit is blocking outbound 5432 or if DB host requires allowlisting.

Verify environment variables

Ensure apps/api reads correct DATABASE_URL at runtime.

Confirm .env or Replit Secrets are loaded for the backend process.

Print safe debug logs (no secrets) showing Prisma is reading DATABASE_URL (only host/port part).

Fix Supabase Prisma connection requirements

Ensure DATABASE_URL includes ?sslmode=require

If Supabase recommends pooler, configure Prisma properly:

Use pooled connection string for runtime (PgBouncer)

Use direct connection string for migrations if needed (via directUrl in schema.prisma)

Confirm Prisma client generation step:

run pnpm -w prisma generate (or npx prisma generate in correct workspace)

Ensure Nest starts after prisma generate completes in build/start script.

Make the run configuration stable

Start backend on 3001 and frontend on 5000 consistently.

Ensure only ONE backend process runs (avoid port conflicts).

Add a health endpoint or simple test route and verify:

curl http://127.0.0.1:3001/api/health returns 200

Once backend stays up, confirm proxy calls succeed:

/api/auth/profile

/api/role-permissions/my

/api/donors?page=1&limit=10

Acceptance:

NestJS backend no longer crashes.

Prisma connects successfully to Supabase DB.

Donors page loads (API returns data) and Dashboard AI Insights endpoint works.

Please implement the minimal changes required (env + prisma + connection string + scripts). Do NOT rewrite the application architecture.

2 quick notes you can tell them verbally in the meet

“This is not a frontend issue. Next proxy is failing only because Nest on 3001 is crashing.”

“Fixing Prisma ↔ Supabase connection will automatically fix donors/insights.”