Proceed with Phase 2.

First, extend and refine the database schema using Prisma to fully support a real-world NGO Donor Management System.

Focus ONLY on backend schema + migrations in this step. Do NOT build UI yet.

Update / add models with proper relations for:

1) Donors
- auto-generated donorCode (AKF-DNR-000001)
- full name split (first, middle, last)
- primaryPhone, alternatePhone, whatsappPhone with country codes
- personalEmail, officialEmail
- address, city, state, country
- profession, approximateAge, gender, incomeSpectrum, religion
- donationMethods (multi-select), donationFrequency
- NGO internal notes
- communicationPreferences (email, whatsapp, sms, reminders)
- timezone (IANA, required for message scheduling)
- category (INDIVIDUAL, NGO, CSR_REP, WHATSAPP_GROUP, SOCIAL_MEDIA_PERSON, CROWD_PULLER, VISITOR_ENQUIRY)
- specialFlags (under18Helper, seniorCitizen, singleParent, disabled)
- sourceOfDonor (social media, justdial, friend, sponsor, other)
- assignedToUserId (telecaller/staff)
- referredByDonorId (self reference)
- profilePicUrl
- allow donor creation with partial data (name OR phone OR email)

2) DonorSpecialOccasions
- donorId
- type (DOB_SELF, DOB_SPOUSE, DOB_CHILD, ANNIVERSARY, DEATH_ANNIVERSARY, OTHER)
- date
- relatedPersonName

3) DonorFamilyMembers
- donorId
- relationType (spouse, child, father, mother, etc)
- name
- DOB optional

4) Donations
- donorId
- donationDate
- donationAmount
- donationType (cash, grocery, medicines, prepared_food, used_items, kind)
- donationMode (cash, upi, gpay, phonepe, cheque, online, bank_transfer)
- transactionId
- remarks
- homeId
- visitedHome boolean
- servedFood boolean
- receiptNumber per India financial year
- receiptPdfUrl

5) Pledges / Promises
- donorId
- expectedStartDate, expectedEndDate
- pledgeAmount
- status (PENDING, FULFILLED, NULLIFIED, POSTPONED)

6) Campaigns (basic structure only)

Add:
- proper indexes for phone, email, donorCode
- soft delete support where applicable
- createdAt / updatedAt everywhere

After schema update:
- generate Prisma migrations
- reseed sample data
- ensure app still runs without breaking login
