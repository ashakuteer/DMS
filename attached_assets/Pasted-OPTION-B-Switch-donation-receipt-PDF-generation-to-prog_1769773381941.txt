OPTION B: Switch donation receipt PDF generation to programmatic PDF (PDFKit) to guarantee a single-page receipt with no overlap and no extra footer pages.

Goal:
- Receipt PDF must ALWAYS be exactly 1 page.
- Footer must appear exactly once.
- No HTML->PDF engine should be used for receipts.

Tasks:

1) Install PDF library:
- Add dependency: pdfkit (and optional: number-to-words or implement INR words helper).

2) Implement ReceiptPdfService using PDFKit:
- Create a function generateDonationReceiptPdf(donationId) that:
  - Loads donation + donor from Prisma.
  - Generates A4 PDF (PDFKit default is A4, set margins manually).
  - Draws text using fixed coordinates and line wrapping:
    - Header: "ASHA KUTEER FOUNDATION (Reg. No: 660/2013)"
    - Body: Donation details in a clean two-column layout:
      Receipt No, Date, Donor Name, Address, Email, Amount (Rs.), Amount in Words, Payment Mode, Purpose.
    - 80G section:
      "PAN No: AADAA4681R | Valid up to: 31st March 2027"
      "This donation is eligible for deduction under Section 80G of the Income Tax Act, 1961. PAN: AADAA4681R (Valid up to 31st March 2027)."
    - Footer (single block at bottom, printed ONCE):
      "No. 2-4-118/21/A, New Sadguru Residency, Swaroop Nagar Colony, Uppal, Hyderabad – 500039, Telangana, India"
      "Phone: +91 9700700700 | +91 8600600600 | Email: info@ashakuteer.org | Website: www.ashakuteer.org"
    - Final lines:
      "Thank you for supporting Asha Kuteer Foundation!"
      "Authorized Signatory"
      "This is a system-generated receipt and does not require a physical signature."

- Ensure all text wraps within page width and never goes beyond bottom margin (reserve footer space).

3) Replace existing HTML-based receipt generation:
- Update GET /api/donations/:id/receipt.pdf to use PDFKit output stream.
- Remove/disable any footerTemplate or HTML-to-PDF logic for receipts.

4) Email attachment:
- When a donation is saved and donor has email, attach the PDF generated by PDFKit.
- Do not block UI; send email asynchronously.
- If email missing, skip gracefully.

5) Verification:
- Generate receipt for a large amount (e.g., Rs. 2,50,000) and long address.
- Confirm PDF is exactly 1 page and footer is printed once.
- Confirm email sends with PDF attachment.

Do NOT change database schema or donation creation logic.
