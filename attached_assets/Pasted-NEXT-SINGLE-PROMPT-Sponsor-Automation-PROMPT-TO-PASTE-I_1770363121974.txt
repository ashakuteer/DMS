NEXT SINGLE PROMPT (Sponsor Automation)
PROMPT TO PASTE IN REPLIT AI:
You are working on the NGO DMS (Next.js app in apps/web + NestJS API in apps/api, Supabase DB).
Implement Sponsor Automation end-to-end for Asha Kuteer Foundation with these requirements:
Goal
We already have Beneficiaries + Sponsorship linking. Now implement:
Monthly Sponsorship Reminders (email automated, WhatsApp manual) + tracking due/overdue + dashboard + analytics.
________________________________________
1) Database / Schema
Add/ensure a sponsorships table (or extend existing) with:
•	id uuid pk
•	donor_id uuid fk donors
•	beneficiary_id uuid fk beneficiaries
•	amount_monthly numeric (nullable if sponsor is in-kind only)
•	sponsorship_type enum: MONTHLY_CASH, MONTHLY_IN_KIND, ONE_TIME, OTHER
•	due_day int default 5 (1–28) = day of month due
•	start_date date
•	end_date date nullable
•	status enum: ACTIVE, PAUSED, ENDED
•	notes text nullable
•	created_at, updated_at
Add sponsorship_payments table to track collections:
•	id uuid
•	sponsorship_id uuid fk
•	donation_id uuid fk donations nullable (link when recorded)
•	month int (1–12)
•	year int
•	amount numeric nullable (for in-kind keep null or store estimated)
•	payment_mode enum (reuse existing)
•	status enum: DUE, PAID, SKIPPED
•	paid_on date nullable
•	created_at
Create indexes for donor_id, beneficiary_id, (year,month), status.
________________________________________
2) Backend API (NestJS)
Create endpoints:
•	GET /api/sponsorships/due?window=7 → list sponsorships that are due within next N days or already overdue
•	POST /api/sponsorships/:id/queue-email → queues reminder email (uses global SMTP settings)
•	POST /api/sponsorships/:id/mark-paid with { donationId?, amount?, paidOn?, mode? }
→ marks the month’s due as PAID and optionally links to a Donation
•	POST /api/sponsorships/:id/skip → mark SKIPPED for that month
•	GET /api/sponsorships/summary → counts: due, overdue, paidThisMonth, activeSponsors
Logic:
•	Determine “this month due record” using (year, month). If missing, auto-create sponsorship_payments row as DUE when queried.
•	Overdue if today > due_day and status still DUE.
•	If sponsorship is MONTHLY_IN_KIND, still create due rows but amount can be null and email text should say “monthly support item(s)”.
________________________________________
3) Email Reminder Job (Automated)
Add cron job in API that runs every hour:
•	If auto-email is enabled in Settings, check sponsorship due window (next 7 days + overdue).
•	Queue emails for those not emailed in last 24h (avoid spamming) using an email_jobs table or reuse existing email jobs infra.
•	Email subject:
o	“Monthly Sponsorship Reminder – <Beneficiary Name>”
•	Email body:
o	Address donor name
o	Mention beneficiary name + home (Orphan Girls / Blind Boys / Old Age)
o	Mention amount (if cash) OR “in-kind monthly sponsorship”
o	Include polite thanks + “If already supported this month, please ignore.”
•	If beneficiary has protect_privacy = true, do NOT show child name; use “one of our children/elders”.
________________________________________
4) WhatsApp Manual Action
For each due sponsorship, provide “Copy WhatsApp Reminder” text (not auto-send).
Include:
•	Donor name
•	Beneficiary name unless privacy protected
•	Amount if cash
•	Asha Kuteer Foundation signature
________________________________________
5) Frontend (Next.js in apps/web/src/app/(dashboard))
Update Daily Actions page:
•	Add a new card section “Monthly Sponsorships Due”
•	Table columns: Sponsor (donor), Beneficiary, Home, Amount, Due Day, Status (Due/Overdue), Actions
•	Actions:
o	WhatsApp icon → copies WhatsApp reminder text (toast: “Message copied”)
o	Email icon → calls queue-email (toast: “Email queued for <Donor>”)
o	Check icon → opens a modal to mark paid (select donation to link OR just mark paid)
o	External link icon → open donor profile
Add to Analytics page:
•	KPI: Active Sponsorships, Total Monthly Sponsorship Value, Overdue Count
•	Donor Segments tab: Sponsorships Due (show table)
________________________________________
6) Donations Linkage
When user records a Donation that is “Sponsorship Monthly” (cash or in-kind):
•	Allow selecting Sponsorship (donor + beneficiary)
•	If selected, auto mark current month sponsorship payment as PAID and link donationId.
If donation is IN_KIND, disable payment mode options except “Cash” should not show; show only: IN_KIND or “Not Applicable”. Keep Quantity/Unit optional.
________________________________________
7) UX / Reliability
•	All list rows must have stable unique React keys (no warnings).
•	Add helpful toasts on success/failure.
•	Ensure API errors are shown in toast with readable message.
•	Add data-testid for the main actions on Sponsorship table.
Finally: run typecheck/build, fix any missing UI components (shadcn) by adding them in apps/web/src/components/ui/* if missing, and ensure imports are correct.
After implementation, tell me clearly:
•	“Sponsor automation completed”
•	What pages changed
•	What endpoints were added
•	How to test quickly in UI

