PROMPT (Replit):

Build the remaining missing parts in my NestJS + Next.js monorepo (apps/api + apps/web). Do NOT refactor unrelated code. Make only required changes.

Goal 1: Fix “Assign To” dropdown in Donor Create form (/dashboard/donors/new).

Currently dropdown shows roles like “Admin User (Admin) / Staff Member (Staff) / Telecaller User (Telecaller)”.

Instead, it must show real staff names from backend.

Use existing endpoint GET /api/users/staff (RequirePermission("DONOR_ASSIGN_OWNER")).

Display list items as: <name> (<role>) and include “Unassigned” and “Other”.

Staff list must include these default staff names: Rajini, Ashwini, Nandini, Sarala, Padma, Revathi, Swapna, plus Admin.

When saving a new donor, store selected staff as assignedToUserId in donor record.

Keep donor assignment also editable later via existing PATCH /api/donors/:id/assign.

Goal 2: Admin Staff Management (new page under Management or Staff & Tasks).
Admin should be able to:

Create staff user (name, phone, email optional, role STAFF or TELECALLER, isActive=true)

Deactivate staff user

Reassign donors from one staff user to another (bulk transfer): move all donors where assignedToUserId=fromId to assignedToUserId=toId.
Add minimal backend endpoints + frontend UI. Secure these actions for Role.ADMIN only (or RequirePermission).

Goal 3: Donor photo

Ensure donor photo upload is supported in donor create/edit.

Store photo URL/path in donor model.

Show donor photo on donor detail page.
(If already implemented, just verify it works end-to-end; don’t remove existing code.)

Goal 4: WhatsApp automation (Twilio Content API)

Backend already exists: POST /api/communications/whatsapp/send-template and webhook /api/webhooks/twilio/status.

Add a UI action after “Donation Saved Successfully”: “Send WhatsApp (Auto)” which calls the backend endpoint using contentSid/template, instead of opening api.whatsapp.com.

Keep the manual WhatsApp option also.

Use E.164 formatting validation and use donor phone number.

Constraints:

Do not change Prisma schema unless truly needed.

Keep existing routes and permissions.

Keep code style consistent.

Provide a short checklist of files changed and how to test each goal.