We already have a working NestJS (API on 3001) + Next.js frontend (dashboard on 5000). Donors list and donor pages are loading correctly.

I want ONLY THREE CHANGES:

------------------------------------------------
1) Assign Owner at Donor Creation Time
------------------------------------------------
Requirement:
- In the "Add New Donor" form UI, add a new field:
  "Assigned To (Owner)"
- Dropdown options come from:
  GET /api/users/staff
- Display staff names.
- Include one option: "Unassigned" (maps to assignedToUserId = null).
- When creating donor (POST /api/donors), send assignedToUserId.
- Backend must save assignedToUserId in donor table and return assignedToUser details.

------------------------------------------------
2) Donor Photo Upload at Creation Time
------------------------------------------------
Requirement:
- In "Add New Donor" form, add field: "Donor Photo"
- Accept: jpg, jpeg, png, webp
- Max size: 3MB
- Backend:
  - Extend donor model with photoUrl (nullable string)
  - Accept multipart/form-data in POST /api/donors
  - Store image in /uploads/donors/
  - Save photoUrl in DB
- Frontend:
  - Preview image before save
  - Show photo in donor detail page

------------------------------------------------
3) Admin: Manage Staff Contact Numbers
------------------------------------------------
Requirement:
- Only ADMIN can do this.
- Add a simple Admin UI under Users/Staff management:
  
  A) Add New Staff Contact Number
  - Admin can add a new staff member with:
    - name
    - role (STAFF / TELECALLER / MANAGER)
    - phone number (unique)
  - This should create a new user in DB.

  B) Reassign Staff Contact Number
  - When a staff stops working:
    - Admin can transfer that staff’s phone number to another existing staff.
  - This updates the phone number field from old staff → new staff.
  - Old staff can be marked inactive.

Constraints:
-----------
- Do NOT refactor existing donor, auth, or permission logic.
- Do NOT change unrelated APIs.
- Keep UI simple (basic form is enough).
- Keep all changes minimal and safe.

Success Criteria:
-----------------
- Admin can add a new staff with phone number.
- Admin can move one staff phone number to another staff.
- Donor creation supports owner assignment + photo upload.
- No existing donor, donation, dashboard pages break.