Single Prompt: Donations + Sponsorship Automation (Copy–Paste into Replit AI)

Use this exactly as one prompt in Replit AI:

Goal

Improve Donations module UX + fully automate Sponsorship workflows with reminders, linking donors to beneficiaries, and reporting.

Donations Module Improvements

Improve “Record Donation” form:

Allow searching donor by mobile number, name, or donor code and auto-select donor.

For in-kind donations (groceries, rice bags, medicines, stationery, sports kits, annadanam):

Disable payment modes like UPI, GPay, Bank Transfer (allow only “In-Kind / Not Applicable”).

Make Quantity and Unit optional.

Keep Estimated Value optional.

For cash donations, keep payment modes enabled and Amount mandatory.

Donation History Actions:

Replace the current “eye/open profile” action with a Donation Details modal showing:

Donor name & code

Donation type (cash / in-kind)

Payment mode

Items/quantity (if in-kind)

Amount / Estimated value

Designated Home (Orphan Girls / Blind Boys / Old Age)

Notes

Add buttons:

“Send Thank You (Email)”

“Send Thank You (WhatsApp – manual)”

Add Export:

Add Export to Excel for Donations with columns:
Date, Receipt No, Donor Code, Donor Name, Phone, Donation Type, Payment Mode, Amount, Items, Quantity, Estimated Value, Designated Home, Notes.

Sponsorship Automation (Donor ↔ Beneficiary Linking)

Sponsorship Data Model:
Create a Sponsorship entity with:

sponsorDonorId

beneficiaryId

monthlyAmount

startDate

dueDayOfMonth

status (ACTIVE, PAUSED, STOPPED)

UI Features:

On Beneficiary Profile:

Add “Link Sponsor” button to attach a donor as sponsor.

Show list of sponsors with monthly amount and due date.

On Donor Profile:

Add “Sponsored Beneficiaries” tab showing linked children/elderly.

Automations:

Add Monthly Sponsorship Due section in Daily Actions dashboard.

Show overdue sponsorships with red status.

Add action buttons:

WhatsApp (manual reminder)

Email reminder (automated using existing SMTP config)

Birthday Automations:

On Donor Birthday:

Show sponsored beneficiaries on Daily Actions.

Add “Generate Birthday Wish Message” including beneficiary name & photo.

On Beneficiary Birthday:

Generate a thank you message to sponsor with beneficiary name + photo (manual WhatsApp + email option).

Beneficiary Enhancements

Add profile photo upload for beneficiaries.

Add Updates & Timeline for beneficiaries:

Allow admin to post updates with photos.

Show these updates inside Donor → Sponsored Beneficiaries tab.

Add “Send Update to Sponsor (Email)” button (manual trigger).

Quality & Stability

Ensure all new features:

Follow existing UI style

Use existing auth & role permissions

Add proper error handling and toast messages

Add loading states for actions