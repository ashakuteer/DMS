Task: Improve Donation Thank-You Email + PDF Receipt Design (NO database/model changes, keep all existing fields and logic)

Context: Donation receipt email is currently plain text and looks unbranded. PDF receipt works but needs professional branding. We want a premium, trustworthy NGO look.

A) Email (HTML template)

Replace the current plain-text donation thank-you email body with a branded responsive HTML email.

Layout requirements:

Soft light background (#f5f7fb style)

Centered white card container (max-width ~600px, padding 24px, rounded corners)

Header bar at top with Asha Kuteer logo + title “Thank you for your donation”

Clear donor greeting: “Dear {{donor_name}},”

Show these donation details in a neat box/table:

Receipt Number: {{receipt_number}}

Donation Amount: ₹{{amount}}

Date: {{donation_date}}

Payment Mode: {{payment_mode}}

80G note in a highlighted section:
“This donation is eligible for tax exemption under Section 80G…”

Footer with organization details (phone, email, website, address).

Keep the PDF receipt attached exactly as before.

Use existing logo file already in repo (do not fetch externally). If email clients can’t load local images directly, embed logo as base64 inline or attach as inline CID image—choose whichever is already used in codebase.

Do not change sending logic, only improve the email HTML template and styling.

B) PDF Receipt (branding + layout only)

Keep the same receipt data fields and values (no model changes).

Improve the PDF design:

Top banner with brand color + Asha Kuteer logo

Title: “Donation Receipt”

Two-column clean layout for receipt metadata (Receipt No, Date, Donor, Amount, Mode, etc.)

Highlight the 80G/tax exemption section with a light background box

Professional spacing, typography, and consistent margins

Footer with org registration/address/website

Output should remain a single PDF attachment, same filename pattern as current.

Important constraints

Do NOT change Prisma schema or any DB models.

Do NOT change business logic (receipt numbering, donation creation, email triggering).

Only update: email HTML template + PDF styling/layout.

Deliverable

Show which files were changed.

Include screenshots or sample render output for the updated email and PDF.

W