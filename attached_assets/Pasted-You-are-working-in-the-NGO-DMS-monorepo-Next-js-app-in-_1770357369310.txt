You are working in the NGO DMS monorepo (Next.js app in /apps/web, NestJS API in /apps/api, Supabase/Postgres DB). Implement “Sponsorship Automation” end-to-end and also fix the React unique key warning on Analytics pledges table.

PART A — Sponsorship Automation (Monthly due reminders + actions)
Goal:
We already have Beneficiaries + Donors and a “Monthly Sponsorships Due” card/table in Daily Actions. Now make it fully functional with:
1) proper DB model (sponsorship links donor <-> beneficiary),
2) due calculation (monthly),
3) actions: WhatsApp deep-link (manual), Email queue (uses SMTP settings), and Mark Paid (records a donation and advances next due date),
4) analytics segment tab “Sponsorships Due” should show correct data.

A1) Database / Types
Create tables (or Prisma models / SQL migrations depending on current project style):
- sponsorships:
  - id (uuid PK)
  - donor_id (uuid FK donors)
  - beneficiary_id (uuid FK beneficiaries)
  - amount_inr (int)
  - frequency (enum: MONTHLY only for now)
  - due_day (int 1-28)  // keep safe for all months
  - start_date (date)
  - next_due_date (date)  // computed & updated
  - status (enum: ACTIVE, PAUSED, ENDED)
  - notes (text nullable)
  - created_at, updated_at
- sponsorship_payments (optional but recommended):
  - id (uuid)
  - sponsorship_id
  - paid_on (date)
  - amount_inr
  - donation_id (uuid, nullable)  // link to recorded donation
  - notes
  - created_at
Add indexes on donor_id, beneficiary_id, next_due_date, status.

A2) API (NestJS)
Add endpoints:
- GET /api/sponsorships?status=ACTIVE&dueWithinDays=7
  returns sponsorship rows joined with donor + beneficiary summary and computed “isOverdue”, “daysUntilDue”
- POST /api/sponsorships  (create link donor<->beneficiary)
- PATCH /api/sponsorships/:id (pause/resume/edit amount/due_day)
- POST /api/sponsorships/:id/mark-paid
  - creates a donation record of type "SPONSORSHIP" (or "CASH" if your donation types are fixed; but keep separate if possible)
  - sets donation.designatedHome = beneficiary.homeType
  - amount = sponsorship.amount_inr
  - paymentMode must be "CASH/UPI/BANK_TRANSFER" etc (but sponsorship is money)
  - records into sponsorship_payments
  - advances next_due_date by 1 month keeping the same due_day (clamp to 28)
Return success toast message text: “Sponsorship marked paid and next due updated.”

A3) Email Job Queue Integration
When user clicks Email icon for a Sponsorship Due row:
- queue an email job using existing email job pipeline (same as pledges/special-days)
- subject: “Monthly Sponsorship Reminder – <Beneficiary Name>”
- body: polite reminder includes donor name, beneficiary name, home type, amount, due date, and a thank-you.
- toast: “Email queued for <Donor Name>”
Also add server validation: if SMTP not configured, return 400 with friendly message and show toast.

A4) WhatsApp (Manual)
WhatsApp icon should open a wa.me deep link with a prefilled message:
- include donor name + beneficiary name + amount + due date + your foundation name.
No automation beyond opening WhatsApp. (Do NOT attempt Twilio.)

A5) UI Updates (Next.js)
- Daily Actions:
  - Ensure “Monthly Sponsorships Due” table renders real data from /api/sponsorships?dueWithinDays=7
  - Add 3 actions per row:
    1) WhatsApp icon (deep link)
    2) Email icon (queue email)
    3) Open Profile icon should open DONOR profile (not beneficiary) in new tab
  - Add a “Mark Paid” ✅ button OR icon inside the row actions (confirm dialog).
- Beneficiary Profile:
  - Sponsors tab: allow linking a donor as sponsor:
    - donor search (name/code/phone)
    - amount (₹)
    - due day (1-28)
    - start date (default today)
    - status ACTIVE
  - Show list of sponsorships for that beneficiary with status and next due.
- Donor Profile:
  - Sponsorships tab already exists; make sure it pulls linked sponsorships and shows beneficiary + next due + amount.

A6) Tests / Seed (light)
- Add minimal seed/demo for 2 sponsorships so Daily Actions shows 2 rows.
- Add at least 1 unit test or integration test for mark-paid advancing next_due_date.

PART B — Fix Analytics React Key Warning
There is a console error:
“Each child in a list should have a unique ‘key’ prop” in src/app/(dashboard)/dashboard/analytics/page.tsx inside PledgesTable where it does:
<tr key={p.id} ...>
Some rows have missing/duplicate p.id. Fix by ensuring stable unique key:
- Prefer p.id if truthy and unique
- else use a composite key like `${p.donorId}-${p.pledgeId ?? ''}-${p.dueDate ?? ''}-${i}`
Do the same for any other .map tables in analytics page if needed.

DELIVERABLES
- All migrations + API + UI implemented
- No build/runtime errors
- Daily Actions shows sponsorship due rows, WhatsApp opens correct prefill, Email queues, Mark Paid works
- Analytics page has zero key warnings
After finishing, print a short checklist of what you changed and which pages to manually verify.
