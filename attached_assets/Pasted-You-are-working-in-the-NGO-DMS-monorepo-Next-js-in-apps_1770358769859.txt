You are working in the NGO DMS monorepo (Next.js in /apps/web, NestJS in /apps/api, Supabase Postgres). Implement “Beneficiary Deep Records + Secure Documents + Reports + Timelines” end-to-end.

IMPORTANT SECURITY / PRIVACY RULES
- Aadhaar, govt IDs, prescriptions, medical info are sensitive. Store files in PRIVATE storage (Supabase Storage private bucket).
- Never expose raw file URLs publicly. Use signed URLs from the API with short expiry.
- Add role-based access control: only ADMIN can upload/view sensitive docs; STAFF can view non-sensitive photos and general updates; DONOR (if exists later) can only see shared updates explicitly marked “shareWithDonor=true” and never see IDs/medical docs.
- Keep an audit trail for sensitive document access (who, when, what).
- Add “Protect privacy” at beneficiary level and per-document level.

PART A — Beneficiary extended profile fields (Join + baseline)
Add fields to beneficiaries table/model:
- date_of_joining (date, required for all beneficiaries)
- height_cm_at_join (int nullable)
- weight_kg_at_join (numeric(5,2) nullable)
- dob (date nullable) OR existing month/day + approx_age (keep existing but add normalized optional dob if possible)
- category (enum: CHILD, VISUALLY_IMPAIRED, SENIOR, OTHER) or reuse existing Category field
- privacy_protected (boolean default false)

Update Add Beneficiary UI to include:
- Date of Joining (required)
- Height at joining (optional)
- Weight at joining (optional)
- Keep current month/day/approx age inputs; if DOB unknown allow month/day only.

PART B — Quarterly Height/Weight Tracking
Create beneficiary_metrics table:
- id uuid
- beneficiary_id uuid FK
- recorded_on date (default today)
- height_cm int nullable
- weight_kg numeric(5,2) nullable
- notes text nullable
- created_by (user id)
- created_at
Add API:
- POST /api/beneficiaries/:id/metrics
- GET /api/beneficiaries/:id/metrics (latest first)
UI:
- Beneficiary Profile tab “Updates” or new tab “Health & Growth”
- Show baseline at joining + timeline chart/list of quarterly records
- Quick button “Add Quarterly Update” (date + height + weight + notes)

PART C — Progress Cards (for children below 10th)
Create progress_cards table:
- id uuid
- beneficiary_id uuid
- academic_year (e.g., "2025-26")
- term (enum: Q1,Q2,Q3,Q4 or Term1/Term2)
- class_grade (text) (e.g., "5th Class")
- school (text nullable)
- overall_percentage numeric(5,2) nullable
- remarks text nullable
- file_attachment_id uuid nullable (link to documents table)
- created_at, created_by
API:
- POST /api/beneficiaries/:id/progress-cards
- GET /api/beneficiaries/:id/progress-cards
UI:
- Beneficiary Profile tab “Progress”
- List by year/term, upload PDF/image if available
- If beneficiary class/role indicates below 10th, show this prominently

PART D — Medical / Health Updates to Donors (manual message + optional email queue)
Create beneficiary_health_events table:
- id uuid
- beneficiary_id uuid
- event_date date
- title text (e.g., “Fever – doctor visit”)
- description text
- severity (LOW/MED/HIGH)
- requires_donor_update boolean default false
- share_with_donor boolean default false (must be true to show/send to donor)
- created_at, created_by
UI:
- Beneficiary Profile tab “Health & Growth”: Health Events section
- Button “Create Health Update”
- If share_with_donor = true, show “Copy WhatsApp update” button and “Queue Email to Sponsors” button
Email:
- queue email jobs to all ACTIVE sponsors of this beneficiary (from sponsorships table)
- subject: “Health Update – <Beneficiary Name>”
- include only safe info, never attach Aadhaar/ID
- toast: “Email queued to <n> sponsor(s)”

PART E — Secure Documents Vault (Beneficiary + Organization)
Create documents table for metadata (NO raw files in DB):
- id uuid
- owner_type enum: BENEFICIARY, ORGANIZATION
- owner_id uuid nullable (beneficiary id for BENEFICIARY, null for ORGANIZATION)
- doc_type enum:
  - BENEFICIARY_PHOTO, PROGRESS_CARD, PRESCRIPTION, AADHAAR, GOVT_ID, OTHER
  - ORG_LEGAL, ORG_GOVT, ORG_AUDIT_REPORT, ORG_ANNUAL_REPORT, ORG_EVENT_PHOTOS, ORG_OTHER
- title text
- description text
- storage_bucket text
- storage_path text
- mime_type text
- size_bytes int
- is_sensitive boolean default false (true for Aadhaar/Govt ID/Prescription)
- share_with_donor boolean default false (only allowed if NOT sensitive)
- created_by, created_at
Add Supabase Storage buckets:
- ngo-private (PRIVATE)
Upload flow:
- POST /api/documents/upload-url (returns signed upload URL or handles direct upload via API)
- POST /api/documents/complete (store metadata after upload)
- GET /api/documents/:id/signed-view (short expiry signed URL, only if user has access)
Access control:
- ADMIN: can view all
- STAFF: can view non-sensitive + beneficiary photos + progress cards
- No one except ADMIN can view is_sensitive docs
Audit:
- document_access_log table: document_id, user_id, accessed_at, action(VIEW/DOWNLOAD)

UI:
1) Beneficiary Profile “Documents” section:
   - Upload buttons: Photo, Progress Card, Prescription, Aadhaar/Govt ID (Aadhaar upload ADMIN only)
   - List documents with icons, tags Sensitive/Shared
2) Organization “Documents” page:
   - categories: Legal, Govt, Audit, Annual Reports, Events Photos
   - Upload and list with filters

PART F — Updates + Timeline with Pictures (Beneficiary + Organization)
Create updates table:
- id uuid
- owner_type enum BENEFICIARY, ORGANIZATION
- owner_id uuid nullable
- update_date date
- title text
- content text (rich text optional)
- share_with_donor boolean default false
- created_by, created_at
Create update_attachments join table:
- update_id uuid
- document_id uuid (must be non-sensitive to share_with_donor)
UI:
- Beneficiary Profile tab “Timeline”: show updates chronologically with photos
- Organization “Timeline” page: events, milestones
- Add update form with image attachments (stored as documents doc_type=ORG_EVENT_PHOTOS or BENEFICIARY_PHOTO)

PART G — Quarterly & Annual Reports to Donors (Email + manual WhatsApp)
Create report_campaigns table:
- id uuid
- name text (e.g., “Quarterly Report Q1 2026”)
- type enum QUARTERLY, ANNUAL
- period_start, period_end dates
- document_id uuid (report PDF in org documents)
- target enum: ALL_DONORS, SPONSORS_ONLY, CUSTOM
- status enum DRAFT, QUEUED, SENT
- created_at, created_by
Add API:
- POST /api/reports/campaigns
- POST /api/reports/campaigns/:id/queue-emails
This should:
- email to donors (ALL_DONORS) OR only donors with active sponsorships (SPONSORS_ONLY)
- attach report as link (signed url) OR if small, include as attachment (prefer link for simplicity)
UI:
- Reports page: create campaign, attach doc, “Queue Email”
- toast: “Queued report email to <n> donors”
Also provide “Copy WhatsApp message” for manual sharing, but do NOT automate WhatsApp.

PART H — Receipt # for Sponsorship Payments and Donations
Ensure donation receipt numbering supports:
- receipt format: AKF-YYYY-YY-000001 (already similar)
- sponsorship mark-paid should create donation + receipt
- store receipt_no on donations table (unique)
- expose receipt in UI and in donor email.

DELIVERABLES
- Migrations + API + UI pages implemented
- Private file storage + signed URL viewing
- Admin-only sensitive docs (Aadhaar, Govt ID, Prescription)
- Beneficiary has joining date + baseline height/weight + quarterly tracking
- Progress cards module + uploads
- Health updates can be shared and emailed to sponsors (non-sensitive)
- Org documents vault + org timeline + report campaigns (quarterly/annual) to donors
- Add minimal seed data for 1 beneficiary with metrics + progress card + a sponsorship + a report doc

After completing, print:
1) Pages to verify
2) New tables created
3) Which roles can see which docs
