You are working on the NGO DMS (Next.js + NestJS API + Supabase/Postgres).
I need a complete “Donations Excel Export” feature that exports ALL donations with proper NGO fields (Cash + In-Kind donations), with filters, and it must work from the Analytics page button “Donations Excel”.

Requirements

Add a backend endpoint:

GET /api/analytics/donations/export

Accept query params:

from (YYYY-MM-DD) optional

to (YYYY-MM-DD) optional

home optional (ORPHAN_GIRLS, BLIND_BOYS, OLD_AGE)

type optional (CASH, GROCERIES, RICE_BAGS, STATIONERY, MEDICINES, ANNADANAM, SPORTS_KITS, OTHER)

Return an .xlsx file download (set correct headers).

Excel columns must be NGO-friendly and cover BOTH money and in-kind:

Receipt No

Donation Date

Donor Code

Donor Name

Donor Phone

Donor Email

Donation Type (Cash/Groceries/Rice Bags/Medicines/Stationery/Annadanam/Sports kits/Other)

Payment Mode (ONLY for cash: Cash/UPI/Bank Transfer/Cheque/etc; for in-kind set as “IN_KIND”)

Amount (₹) (for cash)

Quantity (optional; blank allowed)

Unit (optional; blank allowed)

Estimated Value (₹) (optional; used mainly for in-kind)

Designated Home (Girls/Blind Boys/Old Age/Not specified)

Notes

Created By (user name/email if available)

Created At

Data rules:

Quantity and Unit must be OPTIONAL (allow nulls).

For in-kind donations, Amount should be blank or 0, and Estimated Value should be used.

For cash donations, Estimated Value can be blank, Amount is used.

Payment Mode dropdown in UI must show ONLY valid options depending on donation type:

If type == CASH → show payment modes

Else → payment mode fixed “IN_KIND” (not editable)

Frontend:

The “Donations Excel” button on /dashboard/analytics must download the file.

Add a small filter modal before export (from/to/home/type). Default = last 12 months.

Use the existing auth token; must work for logged-in admin.

Implementation details:

Use exceljs (preferred) or xlsx to generate workbook server-side.

Ensure large exports don’t crash: stream buffer response properly.

Add basic tests or at least a quick manual test guide in a comment.

Also fix the React warning on analytics page:

“Each child in a list should have a unique key prop” in PledgesTable at src/app/(dashboard)/dashboard/analytics/page.tsx.

Ensure the <tr key=...> uses a truly unique key: p.id ?? \${p.donorId}-${p.pledgeId}-${i}`` or similar.

After implementing, show me:

files changed list

the endpoint path

how to run and verify export