{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPath": "apps/api"
  },
  "deploy": {
    "startCommand": "npx prisma generate --schema=./prisma/schema.prisma && node scripts/ensure-tables.js && node --max-old-space-size=512 dist/src/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
