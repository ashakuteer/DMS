[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"

[deploy]
startCommand = "node_modules/.bin/prisma migrate deploy --schema=prisma/schema.prisma && node --max-old-space-size=512 dist/src/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
