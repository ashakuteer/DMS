[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"

[deploy]
startCommand = "node --max-old-space-size=512 dist/src/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
