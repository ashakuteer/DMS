export declare function getSponsorshipDueTemplate(donorName: string, beneficiaryName: string, amount: string, homeName: string, daysUntil: number, org: any): {
    subject: string;
    body: string;
};
